# osm_to_wbt
Pipeline for converting OpenStreetMap data into Webots simulation worlds (.wbt), with iterative enrichment — aligning OSM objects to generated WBT entities, extracting semantic metadata, and progressively improving the world file to produce a high-fidelity urban digital twin.
